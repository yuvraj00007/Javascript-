const id = Symbol('123')
const anotherId = Symbol('123')
console.log(id== anotherId)   // unique hi bna deta h Symbol , andar chehe same cheez pass kari ho 
                              // isiliye ans alag a rha h mtlb false

                              //array 
let heroes=["thor","loki"]

//object:

let myObj={
    name: "yuvraj",
    age : 21,
}

//function :

let MyFunction = function(){
    console.log("yuvraj")
}
console.log(myObj)